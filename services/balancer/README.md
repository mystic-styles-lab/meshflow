# 🔷 Marzban Proxy Balancer v2.0

Продвинутый балансировщик прокси для Marzban с умной балансировкой нагрузки и красивой веб-панелью управления в стиле Discord/Marzban.

![Version](https://img.shields.io/badge/version-2.0.0-blue)
![Node](https://img.shields.io/badge/node-%3E%3D16.0.0-green)
![License](https://img.shields.io/badge/license-MIT-blue)

## ✨ Основные возможности

### 🧠 Умная балансировка нагрузки
- **Алгоритм взвешенного выбора** - учитывает множество факторов:
  - Текущая нагрузка (активные соединения)
  - Здоровье прокси (health checks)
  - Приоритет прокси
  - Среднее время отклика
  - Процент успешных соединений
- **Автоматические health checks** каждые 2 минуты
- **Graceful degradation** - автоматическое исключение неработающих прокси

### 🎨 Современная веб-панель
- Дизайн в стиле Marzban/Discord
- Темная тема с плавными анимациями
- Адаптивный дизайн для мобильных устройств
- Обновление статистики в реальном времени (каждые 5 секунд)

### 🔐 Безопасность
- Аутентификация через сессии
- Хеширование паролей (bcrypt)
- Защита от CSRF
- Возможность смены пароля

### 📊 Мониторинг и статистика
- Детальная статистика по каждому прокси
- Общий дашборд с ключевыми метриками
- Метрики в формате Prometheus
- История соединений в БД

### 💾 База данных
- SQLite для хранения конфигурации
- История статистики
- Управление пользователями
- Миграции при обновлении

## 📸 Скриншоты

### Панель управления
- Статистика в реальном времени
- Карточки прокси с детальной информацией
- Удобное добавление/редактирование

### Логин
- Минималистичный дизайн
- Валидация полей

## 🚀 Быстрый старт

### На Linux сервере

```bash
# 1. Скачать или клонировать проект
cd /opt
# ... загрузить файлы ...

# 2. Запустить установку
chmod +x install.sh
./install.sh

# 3. Настроить .env
nano .env
# Измените ADMIN_PASSWORD!

# 4. Запустить
npm start
```

### На Windows (для разработки)

```powershell
# 1. Установить зависимости
npm install
cd client
npm install
cd ..

# 2. Запустить в режиме разработки
# Терминал 1 - Backend
npm run dev

# Терминал 2 - Frontend  
npm run client
```

Подробные инструкции: [INSTALL.md](INSTALL.md)

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
# Порт SOCKS5 балансировщика (только localhost)
PORT=7777

# Порт веб-панели и API (доступен извне)
METRICS_PORT=9000

# Логин администратора
ADMIN_USERNAME=admin

# Пароль администратора (ОБЯЗАТЕЛЬНО измените!)
ADMIN_PASSWORD=your_secure_password_here

# Секретный ключ для сессий (генерируется автоматически)
SESSION_SECRET=your_random_secret_key

# Окружение (production/development)
NODE_ENV=production
```

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/check` - Проверка сессии
- `POST /api/auth/change-password` - Смена пароля

### Управление прокси
- `GET /api/proxies` - Список всех прокси
- `POST /api/proxies` - Добавить прокси
- `PUT /api/proxies/:id` - Обновить прокси
- `DELETE /api/proxies/:id` - Удалить прокси
- `POST /api/proxies/:id/toggle` - Вкл/Выкл прокси

### Статистика
- `GET /api/stats` - Общая статистика
- `GET /api/metrics` - Метрики Prometheus

## 🏗️ Архитектура

```
proxy-balancer/
├── server.js           # Главный сервер (SOCKS5 + HTTP API)
├── balancer.js         # Умный балансировщик нагрузки
├── database.js         # Работа с SQLite БД
├── load-balancer.js    # Старая версия (устарела)
├── package.json        # Backend зависимости
├── .env                # Конфигурация (не в git)
├── data/               # База данных SQLite
│   └── proxy-balancer.db
└── client/             # React фронтенд
    ├── public/
    ├── src/
    │   ├── components/
    │   │   ├── Dashboard.js    # Главная панель
    │   │   ├── Login.js        # Страница входа
    │   │   ├── ProxyList.js    # Список прокси
    │   │   ├── ProxyModal.js   # Форма добавления/редактирования
    │   │   └── Statistics.js   # Виджеты статистики
    │   ├── api.js             # API клиент
    │   ├── App.js             # Роутинг
    │   └── index.js           # Entry point
    └── package.json           # Frontend зависимости
```

## 🧪 Технологии

### Backend
- **Node.js** - Runtime
- **Express** - HTTP сервер и API
- **better-sqlite3** - База данных
- **socks** - SOCKS5 клиент
- **bcryptjs** - Хеширование паролей
- **express-session** - Управление сессиями

### Frontend
- **React 18** - UI библиотека
- **React Router** - Роутинг
- **Axios** - HTTP клиент
- **Pure CSS** - Стилизация (без UI библиотек)

## 📈 Алгоритм балансировки

Балансировщик использует взвешенный алгоритм выбора прокси:

```javascript
weight = healthScore * 0.3 +      // Здоровье (30%)
         loadFactor * 0.25 +       // Текущая нагрузка (25%)
         successRate * 0.25 +      // Процент успеха (25%)
         priorityScore * 0.1 +     // Приоритет (10%)
         responseScore * 0.1       // Время отклика (10%)
```

Прокси с наибольшим весом выбирается для нового соединения.

## 🔄 Миграция со старой версии

```bash
# 1. Остановить старый балансировщик
ps aux | grep "load-balancer.js"
kill <PID>

# 2. Сохранить конфигурацию прокси
# Запишите IP, порты, логины/пароли ваших прокси

# 3. Установить новую версию
cd /opt/marzban-balancer
./install.sh

# 4. Запустить новую версию
npm start

# 5. Войти в панель и добавить прокси через UI
# http://your-server:9000
```

## 🛡️ Безопасность

### Рекомендации

1. **Измените пароль по умолчанию** сразу после установки
2. **Используйте сильный SESSION_SECRET** (генерируется автоматически при установке)
3. **Настройте firewall**:
   ```bash
   # Разрешить панель только с вашего IP
   ufw allow from YOUR_IP to any port 9000
   
   # Порт 7777 уже слушает только localhost
   ```
4. **Регулярно обновляйте** зависимости:
   ```bash
   npm update
   cd client && npm update
   ```

## 📝 Логирование

### Системные логи (если через systemd)
```bash
sudo journalctl -u proxy-balancer -f
```

### События в коде
- Новые соединения
- Выбор прокси
- Ошибки подключения
- Health checks
- Загрузка/перезагрузка конфигурации

## 🐛 Решение проблем

### Порт уже занят
```bash
sudo lsof -i :7777
sudo kill <PID>
```

### База данных заблокирована
```bash
sudo systemctl stop proxy-balancer
rm -f data/proxy-balancer.db-*
sudo systemctl start proxy-balancer
```

### Фронтенд не собирается
```bash
cd client
rm -rf node_modules package-lock.json
npm install
npm run build
```

## 🤝 Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue.

## 📄 Лицензия

MIT

## 🙏 Благодарности

- Marzban team за вдохновение
- Сообщество Node.js

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте [INSTALL.md](INSTALL.md)
2. Посмотрите логи
3. Создайте issue с описанием проблемы

---

**Сделано с ❤️ для Marzban**
